# Use reviewed immutable image digests in production, never floating latest tags.
POSTGRES_IMAGE=postgres:18.4@sha256:REPLACE
CLICKHOUSE_IMAGE=clickhouse/clickhouse-server:REPLACE@sha256:REPLACE
REDIS_IMAGE=redis:REPLACE@sha256:REPLACE
