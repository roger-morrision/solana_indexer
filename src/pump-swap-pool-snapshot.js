import crypto from "node:crypto";
import fs from "node:fs/promises";
import path from "node:path";
import process from "node:process";
import { fileURLToPath } from "node:url";
import { loadConfig } from "./config.js";
import { IndexStore } from "./store.js";
import { LocalValidatorClient, MAINNET_GENESIS_HASH } from "./local-validator-exporter.js";
import { getMultipleAccountsBatched } from "./rpc-account-batch.js";
import { acquirePoolMintEvidence, bindPoolMintEvidence } from "./pool-mint-evidence.js";

export const PUMP_SWAP_PROGRAM = "pAMMBay6oceH9fJKBRHGP5D4bD4sWpmSwMn52FMfXEA";
export const PUMP_PROGRAM = "6EF8rrecthR5Dkzon8Nwu78hRvfCKubJ14M5uBEwF6P";
export const PUMP_FEE_PROGRAM = "pfeeUxB6jkeY1Hxd7CsFCAjcbHA9rWtchMGdZ6VojVZ";
export const ASSOCIATED_TOKEN_PROGRAM = "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL";
const POOL_DISCRIMINATOR = Buffer.from([241, 154, 109, 4, 17, 177, 109, 188]);
const FEE_CONFIG_DISCRIMINATOR = Buffer.from([143, 52, 146, 187, 219, 123, 76, 155]);
const GLOBAL_CONFIG_DISCRIMINATOR = Buffer.from([149, 8, 156, 202, 160, 252, 176, 217]);
const POOL_ACCOUNT_LENGTH = 261;
const GLOBAL_CONFIG_ACCOUNT_LENGTH = 940;
const MAX_U64 = 18_446_744_073_709_551_615n;
const FEE_BASIS_POINTS = 10_000n;
const DEFAULT_PUBLIC_KEY = "11111111111111111111111111111111";
const SPL_TOKEN_PROGRAM = "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA";
const WRAPPED_SOL_MINT = "So11111111111111111111111111111111111111112";

function base58(bytes) { const alphabet = "123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz"; let value = 0n, output = ""; for (const byte of bytes) value = value * 256n + BigInt(byte); while (value) { output = alphabet[Number(value % 58n)] + output; value /= 58n; } for (const byte of bytes) { if (byte) break; output = `1${output}`; } return output || "1"; }
function base58Bytes(value) { const alphabet = "123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz", indexes = new Map([...alphabet].map((character, index) => [character, index])); if (typeof value !== "string" || !value) throw new Error("Solana address is invalid"); let number = 0n; for (const character of value) { const digit = indexes.get(character); if (digit == null) throw new Error("Solana address is invalid"); number = number * 58n + BigInt(digit); } const bytes = []; while (number) { bytes.unshift(Number(number & 255n)); number >>= 8n; } for (const character of value) { if (character !== "1") break; bytes.unshift(0); } const decoded = Buffer.from(bytes); if (decoded.length !== 32 || base58(decoded) !== value) throw new Error("Solana address is invalid"); return decoded; }
const ED25519_P = (1n << 255n) - 19n;
function mod(value) { const result = value % ED25519_P; return result < 0n ? result + ED25519_P : result; }
function modPow(base, exponent) { let result = 1n, factor = mod(base), power = exponent; while (power) { if (power & 1n) result = mod(result * factor); factor = mod(factor * factor); power >>= 1n; } return result; }
function onEd25519Curve(encoded) { let y = 0n; for (let index = 31; index >= 0; index--) y = (y << 8n) | BigInt(encoded[index]); const sign = y >> 255n; y &= (1n << 255n) - 1n; if (y >= ED25519_P) return false; const y2 = mod(y * y), d = mod(-121665n * modPow(121666n, ED25519_P - 2n)), x2 = mod((y2 - 1n) * modPow(d * y2 + 1n, ED25519_P - 2n)); return x2 === 0n ? sign === 0n : modPow(x2, (ED25519_P - 1n) / 2n) === 1n; }
function findProgramAddress(seeds, programId) { const program = base58Bytes(programId); for (let bump = 255; bump >= 0; bump--) { const digest = crypto.createHash("sha256").update(Buffer.concat([...seeds, Buffer.from([bump]), program, Buffer.from("ProgramDerivedAddress")])).digest(); if (!onEd25519Curve(digest)) return { address: base58(digest), bump }; } throw new Error("unable to derive Solana program address"); }
export function derivePumpPoolAuthority(baseMint) { return findProgramAddress([Buffer.from("pool-authority"), base58Bytes(baseMint)], PUMP_PROGRAM); }
export function derivePumpBondingCurve(baseMint) { return findProgramAddress([Buffer.from("bonding-curve"), base58Bytes(baseMint)], PUMP_PROGRAM); }
export function derivePumpGlobal() { return findProgramAddress([Buffer.from("global")], PUMP_PROGRAM); }
export function derivePumpCreatorVault(creator) { return findProgramAddress([Buffer.from("creator-vault"), base58Bytes(creator)], PUMP_PROGRAM); }
export function derivePumpSharingConfig(baseMint) { return findProgramAddress([Buffer.from("sharing-config"), base58Bytes(baseMint)], PUMP_FEE_PROGRAM); }
export function derivePumpUserVolumeAccumulator(user) { return findProgramAddress([Buffer.from("user_volume_accumulator"), base58Bytes(user)], PUMP_PROGRAM); }
export function derivePumpEventAuthority() { return findProgramAddress([Buffer.from("__event_authority")], PUMP_PROGRAM); }
export function derivePumpFeeConfig(programId) { return findProgramAddress([Buffer.from("fee_config"), base58Bytes(programId)], PUMP_FEE_PROGRAM); }
export function derivePumpSwapFeeConfig() { return derivePumpFeeConfig(PUMP_SWAP_PROGRAM); }
export function derivePumpSwapGlobalConfig() { return findProgramAddress([Buffer.from("global_config")], PUMP_SWAP_PROGRAM); }
export function derivePumpSwapEventAuthority() { return findProgramAddress([Buffer.from("__event_authority")], PUMP_SWAP_PROGRAM); }
export function derivePumpSwapGlobalVolumeAccumulator() { return findProgramAddress([Buffer.from("global_volume_accumulator")], PUMP_SWAP_PROGRAM); }
export function derivePumpSwapUserVolumeAccumulator(user) { return findProgramAddress([Buffer.from("user_volume_accumulator"), base58Bytes(user)], PUMP_SWAP_PROGRAM); }
export function derivePumpSwapCoinCreatorVaultAuthority(coinCreator) { return findProgramAddress([Buffer.from("creator_vault"), base58Bytes(coinCreator)], PUMP_SWAP_PROGRAM); }
export function derivePumpSwapPoolV2(baseMint) { return findProgramAddress([Buffer.from("pool-v2"), base58Bytes(baseMint)], PUMP_SWAP_PROGRAM); }
export function deriveAssociatedTokenAddress({ owner, mint, tokenProgram = SPL_TOKEN_PROGRAM }) { return findProgramAddress([base58Bytes(owner), base58Bytes(tokenProgram), base58Bytes(mint)], ASSOCIATED_TOKEN_PROGRAM); }
function accountBytes(account, label) { if (!Array.isArray(account?.data) || account.data[1] !== "base64" || typeof account.data[0] !== "string") throw new Error(`${label} must use base64 encoding`); return Buffer.from(account.data[0], "base64"); }
function i128(data, offset) { const value = (data.readBigUInt64LE(offset + 8) << 64n) | data.readBigUInt64LE(offset); return (value >= (1n << 127n) ? value - (1n << 128n) : value).toString(); }
function exact(value, label, positive = false) { if (typeof value !== "string" || !/^\d+$/.test(value) || (positive && value === "0")) throw new Error(`${label} must be an exact ${positive ? "positive " : ""}integer string`); return BigInt(value); }
function fees(value, label) { const rates = ["lpFeeBps", "protocolFeeBps", "creatorFeeBps"].map((field) => exact(value?.[field], `${label} ${field}`)); if (rates.some((rate) => rate > 10_000n) || rates.reduce((sum, rate) => sum + rate, 0n) >= 10_000n) throw new Error(`${label} has invalid basis points`); return { lpFeeBps: rates[0].toString(), protocolFeeBps: rates[1].toString(), creatorFeeBps: rates[2].toString() }; }
function ceilDiv(value, divisor) { if (divisor <= 0n) throw new Error("PumpSwap quote divisor must be positive"); return (value + divisor - 1n) / divisor; }
function ceilFee(amount, basisPoints) { return ceilDiv(amount * basisPoints, FEE_BASIS_POINTS); }

export function pumpSwapPoolMarketCap({ baseMintSupplyRaw, baseReserveRaw, quoteReserveRaw }) { const supply = exact(baseMintSupplyRaw, "PumpSwap base mint supply", true), base = exact(baseReserveRaw, "PumpSwap base reserve", true), quote = exact(quoteReserveRaw, "PumpSwap quote reserve"); return (quote * supply / base).toString(); }

export function selectPumpSwapFees({ feeConfig, canonicalPumpPool, baseMintSupplyRaw, baseReserveRaw, quoteReserveRaw }) {
  if (typeof canonicalPumpPool !== "boolean" || !feeConfig) throw new Error("PumpSwap fee selection requires canonical identity and fee config");
  if (!canonicalPumpPool) return { source: "flat", marketCapLamportsRaw: null, ...fees(feeConfig.flatFees, "PumpSwap flat fees") };
  if (!Array.isArray(feeConfig.feeTiers) || !feeConfig.feeTiers.length || feeConfig.feeTiers.length > 256) throw new Error("PumpSwap canonical fee tiers are missing or excessive");
  const tiers = feeConfig.feeTiers.map((tier, index) => ({ threshold: exact(tier?.marketCapLamportsThresholdRaw, `PumpSwap fee tier ${index} threshold`), value: fees(tier?.fees, `PumpSwap fee tier ${index}`) })); for (let index = 1; index < tiers.length; index++) if (tiers[index].threshold <= tiers[index - 1].threshold) throw new Error("PumpSwap fee tiers must be strictly ascending");
  const marketCap = BigInt(pumpSwapPoolMarketCap({ baseMintSupplyRaw, baseReserveRaw, quoteReserveRaw })); let selected = tiers[0]; for (const tier of tiers) if (marketCap >= tier.threshold) selected = tier; else break; return { source: "market_cap_tier", marketCapLamportsRaw: marketCap.toString(), thresholdRaw: selected.threshold.toString(), ...selected.value };
}

export function quotePumpSwapSnapshotExactInput({ snapshot, poolAddress, inputMint, amountIn, now = Date.now(), staleAfterMs = 60_000 }) {
  const amount = exact(amountIn, "PumpSwap amountIn", true); if (amount > MAX_U64) throw new Error("PumpSwap amountIn exceeds u64"); const pool = snapshot?.pools?.find((row) => row.address === poolAddress);
  if (snapshot?.type !== "pump_swap_pool_snapshot" || snapshot.commitment !== "finalized" || !Number.isSafeInteger(snapshot.stateSlot) || !Number.isSafeInteger(snapshot.balanceSlot) || !Number.isSafeInteger(snapshot.configSlot) || snapshot.stateSlot > snapshot.balanceSlot || snapshot.balanceSlot > snapshot.configSlot || !pool || pool.feeConfigSlot !== snapshot.configSlot) throw new Error("PumpSwap quote requires a finalized coherent snapshot");
  const ageMs = now - Date.parse(snapshot.observedAt ?? ""); if (!Number.isFinite(ageMs) || ageMs < 0 || ageMs > staleAfterMs) throw new Error("PumpSwap snapshot is stale or future-dated");
  if (pool.tokenProgram0 !== SPL_TOKEN_PROGRAM || pool.tokenProgram1 !== SPL_TOKEN_PROGRAM) throw new Error("PumpSwap quote does not support Token-2022 transfer-fee uncertainty");
  const quoteToBase = inputMint === pool.tokenMint1 ? true : inputMint === pool.tokenMint0 ? false : null; if (quoteToBase == null) throw new Error("PumpSwap input mint does not belong to pool");
  const baseReserve = exact(pool.vault0AmountRaw, "PumpSwap base reserve", true), realQuoteReserve = exact(pool.vault1AmountRaw, "PumpSwap quote reserve", true), virtualQuote = BigInt(pool.virtualQuoteReservesRaw ?? "invalid"), effectiveQuoteReserve = realQuoteReserve + virtualQuote; if (effectiveQuoteReserve <= 0n) throw new Error("PumpSwap effective quote reserve must be positive");
  const selected = selectPumpSwapFees({ feeConfig: pool.feeConfig, canonicalPumpPool: pool.canonicalPumpPool, baseMintSupplyRaw: pool.baseMintSupplyRaw, baseReserveRaw: baseReserve.toString(), quoteReserveRaw: effectiveQuoteReserve.toString() }), lpRate = BigInt(selected.lpFeeBps), protocolRate = BigInt(selected.protocolFeeBps), creatorRate = pool.coinCreator === DEFAULT_PUBLIC_KEY ? 0n : BigInt(selected.creatorFeeBps);
  const globalConfig = pool.globalConfig; if (!globalConfig || pool.globalConfigSlot !== snapshot.configSlot) throw new Error("PumpSwap quote requires finalized GlobalConfig evidence"); const disabledMask = quoteToBase ? 1 << 3 : 1 << 4; if ((globalConfig.disableFlags & disabledMask) !== 0) throw new Error(`PumpSwap ${quoteToBase ? "buy" : "sell"} is disabled`); if (pool.tokenMint1 === WRAPPED_SOL_MINT && BigInt(globalConfig.buybackBasisPoints) > 0n) throw new Error("PumpSwap SOL buyback fee quoting is unsupported"); if (pool.cashbackCoin && globalConfig.isCashbackEnabled) throw new Error("PumpSwap cashback quoting is unsupported");
  let output, grossQuote, curveQuote, lpFee, protocolFee, creatorFee;
  if (quoteToBase) {
    const totalRate = lpRate + protocolRate + creatorRate; curveQuote = amount * FEE_BASIS_POINTS / (FEE_BASIS_POINTS + totalRate); lpFee = ceilFee(curveQuote, lpRate); protocolFee = ceilFee(curveQuote, protocolRate); creatorFee = ceilFee(curveQuote, creatorRate); const total = curveQuote + lpFee + protocolFee + creatorFee; if (total > amount) curveQuote -= total - amount; if (curveQuote <= 1n) throw new Error("PumpSwap quote input is consumed by fees and rounding"); const invariantInput = curveQuote - 1n; output = baseReserve * invariantInput / (effectiveQuoteReserve + invariantInput); grossQuote = curveQuote; if (output <= 0n || output >= baseReserve) throw new Error("PumpSwap quote has insufficient base liquidity");
  } else {
    grossQuote = effectiveQuoteReserve * amount / (baseReserve + amount); lpFee = ceilFee(grossQuote, lpRate); protocolFee = ceilFee(grossQuote, protocolRate); creatorFee = ceilFee(grossQuote, creatorRate); if (realQuoteReserve < grossQuote - lpFee) throw new Error("PumpSwap real quote reserves cannot cover sell output"); output = grossQuote - lpFee - protocolFee - creatorFee; curveQuote = grossQuote; if (output <= 0n) throw new Error("PumpSwap sell output is consumed by fees");
  }
  if (output > MAX_U64) throw new Error("PumpSwap output exceeds u64"); return { schemaVersion: 1, protocol: "pump-swap", status: "quoted", executable: false, safeForAutomation: false, pool: pool.address, inputMint, outputMint: quoteToBase ? pool.tokenMint0 : pool.tokenMint1, direction: quoteToBase ? "quote_to_base" : "base_to_quote", amountInRaw: amount.toString(), amountOutRaw: output.toString(), curveQuoteAmountRaw: curveQuote.toString(), lpFeeRaw: lpFee.toString(), protocolFeeRaw: protocolFee.toString(), creatorFeeRaw: creatorFee.toString(), baseReserveRaw: baseReserve.toString(), effectiveQuoteReserveRaw: effectiveQuoteReserve.toString(), realQuoteReserveRaw: realQuoteReserve.toString(), feeSource: selected.source, marketCapLamportsRaw: selected.marketCapLamportsRaw, stateSlot: snapshot.stateSlot, balanceSlot: snapshot.balanceSlot, configSlot: snapshot.configSlot, observedAt: snapshot.observedAt, missing: ["local_simulation", "external_signer_approval", "landed_transaction_confirmation"] };
}

export function decodePumpSwapGlobalConfigAccount(address, account) {
  if (account?.owner !== PUMP_SWAP_PROGRAM) throw new Error(`PumpSwap GlobalConfig ${address} has unexpected owner`); const data = accountBytes(account, `PumpSwap GlobalConfig ${address}`); if (data.length !== GLOBAL_CONFIG_ACCOUNT_LENGTH || !data.subarray(0, 8).equals(GLOBAL_CONFIG_DISCRIMINATOR)) throw new Error(`PumpSwap GlobalConfig ${address} has invalid data`); if (data[56] > 31 || data[417] > 1 || data[642] > 1 || data[939] > 1) throw new Error(`PumpSwap GlobalConfig ${address} has invalid flags`); const lpFeeBasisPoints = data.readBigUInt64LE(40), protocolFeeBasisPoints = data.readBigUInt64LE(48), coinCreatorFeeBasisPoints = data.readBigUInt64LE(313), buybackBasisPoints = data.readBigUInt64LE(899), addresses = (offset, count) => Array.from({ length: count }, (_, index) => base58(data.subarray(offset + index * 32, offset + (index + 1) * 32))); if ([lpFeeBasisPoints, protocolFeeBasisPoints, coinCreatorFeeBasisPoints, buybackBasisPoints].some((rate) => rate > FEE_BASIS_POINTS)) throw new Error(`PumpSwap GlobalConfig ${address} has invalid fee basis points`); return { address, admin: base58(data.subarray(8, 40)), lpFeeBasisPoints: lpFeeBasisPoints.toString(), protocolFeeBasisPoints: protocolFeeBasisPoints.toString(), disableFlags: data[56], protocolFeeRecipients: addresses(57, 8), coinCreatorFeeBasisPoints: coinCreatorFeeBasisPoints.toString(), adminSetCoinCreatorAuthority: base58(data.subarray(321, 353)), whitelistPda: base58(data.subarray(353, 385)), reservedFeeRecipient: base58(data.subarray(385, 417)), mayhemModeEnabled: data[417] === 1, reservedFeeRecipients: addresses(418, 7), isCashbackEnabled: data[642] === 1, buybackFeeRecipients: addresses(643, 8), buybackBasisPoints: buybackBasisPoints.toString(), boostAuthority: base58(data.subarray(907, 939)), boostEnabled: data[939] === 1, rawPayloadHash: crypto.createHash("sha256").update(data).digest("hex") };
}

export function decodePumpSwapFeeConfigAccount(address, account) {
  if (account?.owner !== PUMP_FEE_PROGRAM) throw new Error(`PumpSwap FeeConfig ${address} has unexpected owner`); const data = accountBytes(account, `PumpSwap FeeConfig ${address}`); if (data.length < 73 || !data.subarray(0, 8).equals(FEE_CONFIG_DISCRIMINATOR)) throw new Error(`PumpSwap FeeConfig ${address} has invalid data`); let cursor = 8;
  const take = (length) => { if (cursor + length > data.length) throw new Error(`PumpSwap FeeConfig ${address} is truncated`); const value = data.subarray(cursor, cursor + length); cursor += length; return value; }, readU64 = () => { const value = data.readBigUInt64LE(cursor); cursor += 8; return value.toString(); }, readU128 = () => { const low = data.readBigUInt64LE(cursor), high = data.readBigUInt64LE(cursor + 8); cursor += 16; return ((high << 64n) | low).toString(); }, readFees = (label) => fees({ lpFeeBps: readU64(), protocolFeeBps: readU64(), creatorFeeBps: readU64() }, label), readTiers = (label) => { if (cursor + 4 > data.length) throw new Error(`PumpSwap FeeConfig ${address} is truncated`); const count = data.readUInt32LE(cursor); cursor += 4; if (count > 256 || cursor + count * 40 > data.length) throw new Error(`PumpSwap FeeConfig ${address} has excessive or truncated tiers`); const rows = Array.from({ length: count }, (_, index) => ({ marketCapLamportsThresholdRaw: readU128(), fees: readFees(`${label} ${index}`) })); for (let index = 1; index < rows.length; index++) if (BigInt(rows[index].marketCapLamportsThresholdRaw) <= BigInt(rows[index - 1].marketCapLamportsThresholdRaw)) throw new Error(`PumpSwap FeeConfig ${address} tiers are not strictly ascending`); return rows; };
  const bump = take(1)[0], admin = base58(take(32)), flatFees = readFees("PumpSwap flat fees"), feeTiers = readTiers("PumpSwap fee tier"), stableFeeTiers = readTiers("PumpSwap stable fee tier"); if (cursor !== data.length) throw new Error(`PumpSwap FeeConfig ${address} has trailing data`); return { address, bump, admin, flatFees, feeTiers, stableFeeTiers, rawPayloadHash: crypto.createHash("sha256").update(data).digest("hex") };
}

export function decodePumpSwapPoolAccount(address, account) {
  if (account?.owner !== PUMP_SWAP_PROGRAM) throw new Error(`PumpSwap pool ${address} has unexpected owner`);
  const data = accountBytes(account, `PumpSwap pool ${address}`);
  if (data.length !== POOL_ACCOUNT_LENGTH || !data.subarray(0, 8).equals(POOL_DISCRIMINATOR)) throw new Error(`PumpSwap pool ${address} has invalid Pool account data`);
  if (data[243] > 1 || data[244] > 1) throw new Error(`PumpSwap pool ${address} has invalid mode flags`);
  return {
    address, programId: PUMP_SWAP_PROGRAM, poolBump: data[8], poolIndex: data.readUInt16LE(9), creator: base58(data.subarray(11, 43)), tokenMint0: base58(data.subarray(43, 75)), tokenMint1: base58(data.subarray(75, 107)), lpMint: base58(data.subarray(107, 139)), tokenVault0: base58(data.subarray(139, 171)), tokenVault1: base58(data.subarray(171, 203)), lpSupplyRaw: data.readBigUInt64LE(203).toString(), coinCreator: base58(data.subarray(211, 243)), mayhemMode: data[243] === 1, cashbackCoin: data[244] === 1, virtualQuoteReservesRaw: i128(data, 245), rawPayloadHash: crypto.createHash("sha256").update(data).digest("hex"),
  };
}

function parsedVault(account, expectedMint, label) {
  const info = account?.data?.parsed?.info, amount = info?.tokenAmount?.amount, decimals = info?.tokenAmount?.decimals;
  if (!account?.owner || info?.mint !== expectedMint || typeof info?.owner !== "string" || !/^\d+$/.test(amount ?? "") || !Number.isInteger(decimals) || decimals < 0 || decimals > 255) throw new Error(`${label} identity mismatch`);
  return { tokenProgram: account.owner, authority: info.owner, amountRaw: String(amount), decimals };
}
function parsedMint(account, expectedProgram, label) { const info = account?.data?.parsed?.info, supply = info?.supply, decimals = info?.decimals; if (account?.owner !== expectedProgram || !/^\d+$/.test(supply ?? "") || !Number.isInteger(decimals) || decimals < 0 || decimals > 255) throw new Error(`${label} identity mismatch`); return { supplyRaw: String(supply), decimals }; }

export async function createPumpSwapPoolSnapshot({ client, pools, automaticMintEvidence = false, genesisHash, observedAt = new Date().toISOString() }) {
  if (!Array.isArray(pools) || !pools.length || new Set(pools).size !== pools.length || pools.some((pool) => typeof pool !== "string" || !pool)) throw new Error("PumpSwap pools must be unique non-empty addresses");
  const stateResponse = await getMultipleAccountsBatched(client, pools, { commitment: "finalized", encoding: "base64" }, { label: "PumpSwap pool" }), stateSlot = stateResponse?.context?.slot;
  if (!Number.isSafeInteger(stateSlot) || stateResponse.value?.length !== pools.length) throw new Error("invalid PumpSwap pool account response");
  const decoded = pools.map((address, index) => decodePumpSwapPoolAccount(address, stateResponse.value[index]));
  const vaultResponse = await getMultipleAccountsBatched(client, decoded.flatMap((row) => [row.tokenVault0, row.tokenVault1, row.tokenMint0]), { commitment: "finalized", encoding: "jsonParsed", minContextSlot: stateSlot }, { label: "PumpSwap vault and base mint" }), balanceSlot = vaultResponse?.context?.slot;
  if (!Number.isSafeInteger(balanceSlot) || balanceSlot < stateSlot || vaultResponse.value?.length !== decoded.length * 3) throw new Error("invalid PumpSwap vault or mint response");
  decoded.forEach((row, index) => { const first = parsedVault(vaultResponse.value[index * 3], row.tokenMint0, `PumpSwap pool ${row.address} base vault`), second = parsedVault(vaultResponse.value[index * 3 + 1], row.tokenMint1, `PumpSwap pool ${row.address} quote vault`), mint = parsedMint(vaultResponse.value[index * 3 + 2], first.tokenProgram, `PumpSwap pool ${row.address} base mint`); if (mint.decimals !== first.decimals) throw new Error(`PumpSwap pool ${row.address} base mint decimals mismatch`); row.tokenProgram0 = first.tokenProgram; row.tokenProgram1 = second.tokenProgram; row.vaultAuthority0 = first.authority; row.vaultAuthority1 = second.authority; row.vault0AmountRaw = first.amountRaw; row.vault1AmountRaw = second.amountRaw; row.mintDecimals0 = first.decimals; row.mintDecimals1 = second.decimals; row.baseMintSupplyRaw = mint.supplyRaw; const authority = derivePumpPoolAuthority(row.tokenMint0); row.canonicalPoolAuthority = authority.address; row.canonicalPoolAuthorityBump = authority.bump; row.canonicalPumpPool = authority.address === row.creator; });
  const feeConfigAddress = derivePumpSwapFeeConfig().address, globalConfigAddress = derivePumpSwapGlobalConfig().address, configResponse = await getMultipleAccountsBatched(client, [feeConfigAddress, globalConfigAddress], { commitment: "finalized", encoding: "base64", minContextSlot: balanceSlot }, { label: "PumpSwap configuration" }), configSlot = configResponse?.context?.slot; if (!Number.isSafeInteger(configSlot) || configSlot < balanceSlot || configResponse.value?.length !== 2) throw new Error("invalid PumpSwap configuration response"); const feeConfig = decodePumpSwapFeeConfigAccount(feeConfigAddress, configResponse.value[0]), globalConfig = decodePumpSwapGlobalConfigAccount(globalConfigAddress, configResponse.value[1]); decoded.forEach((row) => { row.feeConfig = feeConfig; row.feeConfigSlot = configSlot; row.globalConfig = globalConfig; row.globalConfigSlot = configSlot; });
  if (automaticMintEvidence) { const evidence = await acquirePoolMintEvidence(client, decoded, configSlot); for (const row of decoded) bindPoolMintEvidence(row, evidence); }
  return { schemaVersion: 1, type: "pump_swap_pool_snapshot", chain: "solana", genesisHash, commitment: "finalized", stateSlot, balanceSlot, configSlot, observedAt, pools: decoded };
}

async function atomicWrite(filename, value) { await fs.mkdir(path.dirname(filename), { recursive: true }); const temporary = `${filename}.${process.pid}.tmp`; await fs.writeFile(temporary, `${JSON.stringify(value)}\n`); await fs.rename(temporary, filename); }
async function main() { const config = loadConfig(), store = new IndexStore(config.dataFile, config.maxTransactions, config.retentionSeconds); await store.load(); const artifactOnly = process.argv.includes("--artifact-only"), requested = process.argv.slice(2).filter((value) => value !== "--artifact-only"), pools = requested.length ? requested : Object.entries(store.state.pools).filter(([, row]) => row.protocol === "pump-swap").map(([address]) => address); if (!pools.length) throw new Error("no PumpSwap pools supplied or discovered"); const client = new LocalValidatorClient(process.env.LOCAL_VALIDATOR_RPC || "http://127.0.0.1:8899"), expected = process.env.INDEXER_EXPECTED_GENESIS_HASH || MAINNET_GENESIS_HASH, genesisHash = await client.assertGenesis(expected), snapshot = await createPumpSwapPoolSnapshot({ client, pools, automaticMintEvidence: true, genesisHash }); if (!artifactOnly) { store.applyPumpSwapPoolSnapshot(snapshot); await store.save(); } await atomicWrite(config.pumpSwapPoolSnapshotFile, snapshot); console.log(JSON.stringify({ stateSlot: snapshot.stateSlot, balanceSlot: snapshot.balanceSlot, pools: snapshot.pools.length, artifactOnly })); }
const invokedFile = process.argv[1] ? path.resolve(process.argv[1]) : ""; if (fileURLToPath(import.meta.url).toLowerCase() === invokedFile.toLowerCase()) main().catch((error) => { console.error(error.stack || error); process.exitCode = 1; });
