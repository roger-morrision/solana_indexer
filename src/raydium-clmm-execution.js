import crypto from "node:crypto";
import { raydiumSqrtPriceX64AtTick } from "./clmm-math.js";
import { RAYDIUM_CLMM_PROGRAM } from "./clmm-pool-snapshot.js";
import { buildUnsignedLegacyTransaction, simulateUnsignedTransaction, verifyFinalizedLandedTransaction, verifySignedTransactionBase64 } from "./transaction-simulation.js";

const TOKEN_PROGRAM = "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA";
const TOKEN_2022_PROGRAM = "TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb";
const MEMO_PROGRAM = "MemoSq4gqABAXKb96qnH8TysNcWxMyWCqXgDLGmfcHr";
const SWAP_V2_DISCRIMINATOR = crypto.createHash("sha256").update("global:swap_v2").digest().subarray(0, 8);
const U64_MAX = (1n << 64n) - 1n, U128_MAX = (1n << 128n) - 1n;

function integer(value, label, maximum) { let parsed; try { parsed = BigInt(value); } catch { throw new Error(`${label} is invalid`); } if (parsed < 0n || parsed > maximum) throw new Error(`${label} is invalid`); return parsed; }
function u64(value) { const buffer = Buffer.alloc(8); buffer.writeBigUInt64LE(value); return buffer; }
function u128(value) { const buffer = Buffer.alloc(16); buffer.writeBigUInt64LE(value & U64_MAX, 0); buffer.writeBigUInt64LE(value >> 64n, 8); return buffer; }
function meta(address, signer, writable) { if (typeof address !== "string" || !address) throw new Error("Raydium execution account is missing"); return { address, signer, writable }; }
function floorDiv(value, divisor) { return Math.floor(value / divisor); }

export function buildRaydiumClmmSwapV2Instruction({ quote, pool, payer, inputTokenAccount, outputTokenAccount, minimumOutputRaw }) {
  if (quote?.protocol !== "raydium-clmm" || quote.status !== "quoted" || quote.fullyConsumed !== true || quote.commitment !== "finalized" || quote.pool !== pool?.address || quote.inputMint !== (quote.zeroForOne ? pool.tokenMint0 : pool.tokenMint1) || quote.outputMint !== (quote.zeroForOne ? pool.tokenMint1 : pool.tokenMint0) || !Number.isInteger(quote.limitTick) || !Array.isArray(quote.crossedTickIndexes) || quote.crossedTickIndexes.length !== quote.crossedTicks || ![quote.stateSlot, quote.balanceSlot, quote.tickArraySlot, quote.ammConfigSlot].every((slot) => Number.isSafeInteger(slot) && slot >= 0) || quote.balanceSlot < quote.stateSlot || quote.tickArraySlot < quote.stateSlot || quote.ammConfigSlot < quote.stateSlot || !Number.isInteger(pool.tick) || !Number.isInteger(pool.tickSpacing) || pool.tickSpacing <= 0 || pool.tickArrayCoverage !== "finalized_program_account_snapshot" || !Array.isArray(pool.tickArrays) || !pool.ammConfig || !pool.observationKey) throw new Error("Raydium execution quote evidence is invalid");
  const feeAware = quote.transferFeeMode === "finalized_epoch_mint_evidence";
  if (quote.transferFeeMode != null && quote.transferFeeMode !== "none" && !feeAware) throw new Error("Raydium transfer-fee evidence is invalid");
  if (feeAware && (!Number.isSafeInteger(quote.mintEvidenceSlot) || quote.mintEvidenceSlot < quote.balanceSlot || !Number.isSafeInteger(quote.epoch) || quote.epoch < 0)) throw new Error("Raydium transfer-fee evidence is invalid");
  const amount = integer(quote.amountSpecifiedRaw, "Raydium amount", U64_MAX), quotedOutput = integer(quote.amountOutRaw, "Raydium quoted output", U64_MAX), minimumOutput = integer(minimumOutputRaw, "Raydium minimum output", U64_MAX), sqrtPriceLimit = integer(quote.sqrtPriceLimitX64, "Raydium price limit", U128_MAX);
  if (amount === 0n || quotedOutput === 0n || minimumOutput === 0n || minimumOutput > quotedOutput || sqrtPriceLimit !== raydiumSqrtPriceX64AtTick(quote.limitTick)) throw new Error("Raydium execution bounds are invalid");
  const width = pool.tickSpacing * 60, currentStart = floorDiv(pool.tick, width) * width, requiredStarts = new Set([currentStart, ...quote.crossedTickIndexes.map((tick) => { if (!Number.isInteger(tick) || tick % pool.tickSpacing !== 0) throw new Error("Raydium crossed-tick evidence is invalid"); return floorDiv(tick, width) * width; })]), selected = pool.tickArrays.filter((array) => requiredStarts.has(array?.startTickIndex) && typeof array.address === "string").sort((left, right) => quote.zeroForOne ? right.startTickIndex - left.startTickIndex : left.startTickIndex - right.startTickIndex);
  if (selected.length !== requiredStarts.size || selected[0]?.startTickIndex !== currentStart || new Set(selected.map((array) => array.address)).size !== selected.length) throw new Error("Raydium execution tick-array path is incomplete");
  const inputVault = quote.zeroForOne ? pool.tokenVault0 : pool.tokenVault1, outputVault = quote.zeroForOne ? pool.tokenVault1 : pool.tokenVault0, inputMint = quote.zeroForOne ? pool.tokenMint0 : pool.tokenMint1, outputMint = quote.zeroForOne ? pool.tokenMint1 : pool.tokenMint0;
  const accounts = [meta(payer, true, true), meta(pool.ammConfig, false, false), meta(pool.address, false, true), meta(inputTokenAccount, false, true), meta(outputTokenAccount, false, true), meta(inputVault, false, true), meta(outputVault, false, true), meta(pool.observationKey, false, true), meta(TOKEN_PROGRAM, false, false), meta(TOKEN_2022_PROGRAM, false, false), meta(MEMO_PROGRAM, false, false), meta(inputMint, false, false), meta(outputMint, false, false)];
  if (pool.bitmapExtension) accounts.push(meta(pool.bitmapExtension.address, false, false)); for (const array of selected) accounts.push(meta(array.address, false, true));
  const data = Buffer.concat([SWAP_V2_DISCRIMINATOR, u64(amount), u64(minimumOutput), u128(sqrtPriceLimit), Buffer.from([1])]);
  return { programId: RAYDIUM_CLMM_PROGRAM, accounts, dataHex: data.toString("hex"), evidence: { pool: pool.address, stateSlot: quote.stateSlot, balanceSlot: quote.balanceSlot, tickArraySlot: quote.tickArraySlot, ammConfigSlot: quote.ammConfigSlot, mintEvidenceSlot: feeAware ? quote.mintEvidenceSlot : null, epoch: feeAware ? quote.epoch : null, transferFeeMode: quote.transferFeeMode ?? "none", inputTransferFeeRaw: quote.inputTransferFeeRaw ?? "0", outputTransferFeeRaw: quote.outputTransferFeeRaw ?? "0", amountInRaw: amount.toString(), quotedOutputRaw: quotedOutput.toString(), minimumOutputRaw: minimumOutput.toString(), limitTick: quote.limitTick, sqrtPriceLimitX64: sqrtPriceLimit.toString(), zeroForOne: quote.zeroForOne, tickArrays: selected.map((array) => array.address) } };
}

export function prepareRaydiumClmmSwapV2Simulation({ quote, pool, payer, inputTokenAccount, outputTokenAccount, inputPreAmountRaw, outputPreAmountRaw, minimumOutputRaw, recentBlockhash }) {
  const instruction = buildRaydiumClmmSwapV2Instruction({ quote, pool, payer, inputTokenAccount, outputTokenAccount, minimumOutputRaw }), transaction = buildUnsignedLegacyTransaction({ feePayer: payer, recentBlockhash, instructions: [instruction] });
  const inputPre = integer(inputPreAmountRaw, "Raydium input balance", U64_MAX), outputPre = integer(outputPreAmountRaw, "Raydium output balance", U64_MAX), amount = integer(quote.amountSpecifiedRaw, "Raydium amount", U64_MAX), minimumOutput = integer(minimumOutputRaw, "Raydium minimum output", U64_MAX), quotedOutput = integer(quote.amountOutRaw, "Raydium quoted output", U64_MAX);
  if (inputPre < amount || outputPre + quotedOutput > U64_MAX) throw new Error("Raydium simulation balance bounds are invalid");
  const prepared = { schemaVersion: 1, type: "raydium_clmm_swap_v2_simulation", protocol: "raydium-clmm", commitment: "finalized", minContextSlot: Math.max(quote.stateSlot, quote.balanceSlot, quote.tickArraySlot, quote.ammConfigSlot, quote.mintEvidenceSlot ?? 0), transaction, instructionEvidence: instruction.evidence, simulationPolicy: { allowedProgramIds: [RAYDIUM_CLMM_PROGRAM], requiredProgramIds: [RAYDIUM_CLMM_PROGRAM], instructionPolicies: transaction.instructionPolicies, accountExpectations: [{ address: inputTokenAccount, mint: quote.inputMint, preAmountRaw: inputPre.toString(), minDeltaRaw: (-amount).toString(), maxDeltaRaw: (-amount).toString() }, { address: outputTokenAccount, mint: quote.outputMint, preAmountRaw: outputPre.toString(), minDeltaRaw: minimumOutput.toString(), maxDeltaRaw: quotedOutput.toString() }] } };
  prepared.preparationHash = crypto.createHash("sha256").update(JSON.stringify(prepared)).digest("hex"); return prepared;
}

export async function simulatePreparedRaydiumClmmSwapV2(client, { preparation, expectedGenesisHash, genesisHash }) {
  const { preparationHash, ...unsignedPreparation } = preparation ?? {}, expectedHash = crypto.createHash("sha256").update(JSON.stringify(unsignedPreparation)).digest("hex");
  if (preparation?.schemaVersion !== 1 || preparation.type !== "raydium_clmm_swap_v2_simulation" || preparation.protocol !== "raydium-clmm" || preparation.commitment !== "finalized" || preparation.transaction?.signed !== false || preparation.transaction.submitted !== false || !Number.isSafeInteger(preparation.minContextSlot) || preparation.minContextSlot < 0 || !preparation.simulationPolicy || preparationHash !== expectedHash) throw new Error("Raydium simulation preparation is invalid");
  const policy = preparation.simulationPolicy, receipt = await simulateUnsignedTransaction(client, { transactionBase64: preparation.transaction.transactionBase64, minContextSlot: preparation.minContextSlot, expectedGenesisHash, genesisHash, allowedProgramIds: policy.allowedProgramIds, requiredProgramIds: policy.requiredProgramIds, instructionPolicies: policy.instructionPolicies, accountExpectations: policy.accountExpectations });
  if (receipt.transactionHash !== preparation.transaction.transactionHash || receipt.messageHash !== preparation.transaction.messageHash || receipt.simulationSlot < preparation.minContextSlot || receipt.messageVersion !== "legacy" || !Array.isArray(receipt.programIds) || receipt.programIds.length !== 1 || receipt.programIds[0] !== RAYDIUM_CLMM_PROGRAM) throw new Error("Raydium simulation receipt does not match preparation");
  const result = { ...receipt, type: "raydium_clmm_swap_v2_simulation_receipt", protocol: "raydium-clmm", preparationHash, preparationMessageHash: preparation.transaction.messageHash, instructionEvidence: preparation.instructionEvidence }; result.receiptHash = crypto.createHash("sha256").update(JSON.stringify(result)).digest("hex"); return result;
}

export function createRaydiumClmmSigningRequest({ preparation, simulationReceipt, allowedFeePayers, maxInputRaw, maxSlippageBps, currentSlot, ttlSlots = 32, maxSimulationAgeSlots = 32 }) {
  if (!Array.isArray(allowedFeePayers) || !allowedFeePayers.length || new Set(allowedFeePayers).size !== allowedFeePayers.length || !Number.isInteger(maxSlippageBps) || maxSlippageBps < 0 || maxSlippageBps > 10_000 || !Number.isSafeInteger(currentSlot) || currentSlot < 0 || currentSlot > Number.MAX_SAFE_INTEGER - 150 || !Number.isSafeInteger(ttlSlots) || ttlSlots < 1 || ttlSlots > 150 || !Number.isSafeInteger(maxSimulationAgeSlots) || maxSimulationAgeSlots < 0 || maxSimulationAgeSlots > 150) throw new Error("Raydium signer policy is invalid");
  const { preparationHash, ...unsignedPreparation } = preparation ?? {}, expectedPreparationHash = crypto.createHash("sha256").update(JSON.stringify(unsignedPreparation)).digest("hex"), payer = preparation?.transaction?.instructionPolicies?.[0]?.accounts?.[0], evidence = preparation?.instructionEvidence;
  const { receiptHash, ...unsignedReceipt } = simulationReceipt ?? {}, expectedReceiptHash = crypto.createHash("sha256").update(JSON.stringify(unsignedReceipt)).digest("hex"); if (preparationHash !== expectedPreparationHash || receiptHash !== expectedReceiptHash || !payer?.signer || !payer.writable || !allowedFeePayers.includes(payer.address) || simulationReceipt?.type !== "raydium_clmm_swap_v2_simulation_receipt" || simulationReceipt.status !== "simulated" || simulationReceipt.submitted !== false || simulationReceipt.signed !== false || simulationReceipt.preparationHash !== preparationHash || simulationReceipt.messageHash !== preparation.transaction.messageHash || simulationReceipt.transactionHash !== preparation.transaction.transactionHash || !Number.isSafeInteger(simulationReceipt.simulationSlot) || currentSlot < simulationReceipt.simulationSlot || currentSlot - simulationReceipt.simulationSlot > maxSimulationAgeSlots) throw new Error("Raydium signing evidence is invalid");
  const amount = integer(evidence?.amountInRaw, "Raydium amount", U64_MAX), maximum = integer(maxInputRaw, "Raydium maximum input", U64_MAX), quoted = integer(evidence?.quotedOutputRaw, "Raydium quoted output", U64_MAX), minimum = integer(evidence?.minimumOutputRaw, "Raydium minimum output", U64_MAX); if (amount === 0n || amount > maximum || quoted === 0n || minimum > quoted) throw new Error("Raydium signer limits are exceeded"); const slippageBps = Number(((quoted - minimum) * 10_000n + quoted - 1n) / quoted); if (slippageBps > maxSlippageBps) throw new Error("Raydium signer limits are exceeded");
  const request = { schemaVersion: 1, type: "raydium_clmm_swap_v2_signing_request", protocol: "raydium-clmm", approvalRequired: true, signingPerformed: false, submissionPerformed: false, feePayer: payer.address, transactionBase64: preparation.transaction.transactionBase64, transactionHash: preparation.transaction.transactionHash, messageHash: preparation.transaction.messageHash, preparationHash, simulationReceiptHash: receiptHash, simulationSlot: simulationReceipt.simulationSlot, currentSlot, expiresSlot: currentSlot + ttlSlots, maxInputRaw: maximum.toString(), amountInRaw: amount.toString(), quotedOutputRaw: quoted.toString(), minimumOutputRaw: minimum.toString(), slippageBps, instructionEvidence: evidence };
  request.signingRequestHash = crypto.createHash("sha256").update(JSON.stringify(request)).digest("hex"); return request;
}

export function verifyRaydiumClmmSignedRequest({ signingRequest, signedTransactionBase64, currentSlot }) {
  const { signingRequestHash, ...unsignedRequest } = signingRequest ?? {}, expectedRequestHash = crypto.createHash("sha256").update(JSON.stringify(unsignedRequest)).digest("hex");
  if (signingRequest?.schemaVersion !== 1 || signingRequest.type !== "raydium_clmm_swap_v2_signing_request" || signingRequest.protocol !== "raydium-clmm" || signingRequest.approvalRequired !== true || signingRequest.signingPerformed !== false || signingRequest.submissionPerformed !== false || signingRequestHash !== expectedRequestHash || !Number.isSafeInteger(currentSlot) || currentSlot < signingRequest.currentSlot || currentSlot > signingRequest.expiresSlot) throw new Error("Raydium signing request is invalid or expired");
  const verified = verifySignedTransactionBase64({ signedTransactionBase64, unsignedTransactionBase64: signingRequest.transactionBase64, expectedMessageHash: signingRequest.messageHash }); if (verified.approvedTransactionHash !== signingRequest.transactionHash || verified.signerAddresses[0] !== signingRequest.feePayer) throw new Error("Raydium signed transaction does not match approval");
  const result = { ...verified, type: "raydium_clmm_swap_v2_signed_transaction", protocol: "raydium-clmm", signingRequestHash, verifiedSlot: currentSlot, expiresSlot: signingRequest.expiresSlot, approvalRequired: false, submissionPerformed: false }; result.signedArtifactHash = crypto.createHash("sha256").update(JSON.stringify(result)).digest("hex"); return result;
}

export async function verifyFinalizedRaydiumClmmSwap(client, { signedArtifact, signingRequest, simulationReceipt, expectedGenesisHash, genesisHash }) {
  const { signedArtifactHash, ...unsignedArtifact } = signedArtifact ?? {}, expectedArtifactHash = crypto.createHash("sha256").update(JSON.stringify(unsignedArtifact)).digest("hex"), { signingRequestHash, ...unsignedRequest } = signingRequest ?? {}, expectedRequestHash = crypto.createHash("sha256").update(JSON.stringify(unsignedRequest)).digest("hex"), { receiptHash, ...unsignedReceipt } = simulationReceipt ?? {}, expectedReceiptHash = crypto.createHash("sha256").update(JSON.stringify(unsignedReceipt)).digest("hex");
  if (signedArtifact?.type !== "raydium_clmm_swap_v2_signed_transaction" || signedArtifact.protocol !== "raydium-clmm" || signedArtifact.status !== "signature_verified" || signedArtifact.submitted !== false || signedArtifact.submissionPerformed !== false || signedArtifactHash !== expectedArtifactHash || signingRequestHash !== expectedRequestHash || receiptHash !== expectedReceiptHash || signedArtifact.signingRequestHash !== signingRequestHash || signingRequest.simulationReceiptHash !== receiptHash || signedArtifact.messageHash !== signingRequest.messageHash || signedArtifact.approvedTransactionHash !== signingRequest.transactionHash || signedArtifact.firstSignature == null) throw new Error("Raydium finalized confirmation chain is invalid");
  const landed = await verifyFinalizedLandedTransaction(client, { signature: signedArtifact.firstSignature, simulationReceipt, expectedGenesisHash, genesisHash }), result = { schemaVersion: 1, type: "raydium_clmm_swap_v2_finalized_confirmation", protocol: "raydium-clmm", status: "finalized", signedByIndexer: false, submittedByIndexer: false, signature: landed.signature, messageHash: landed.messageHash, simulationSlot: landed.simulationSlot, finalizedSlot: landed.finalizedSlot, preparationHash: signingRequest.preparationHash, simulationReceiptHash: receiptHash, signingRequestHash, signedArtifactHash };
  result.confirmationHash = crypto.createHash("sha256").update(JSON.stringify(result)).digest("hex"); return result;
}

export const RAYDIUM_CLMM_EXECUTION_CONSTANTS = Object.freeze({ tokenProgram: TOKEN_PROGRAM, token2022Program: TOKEN_2022_PROGRAM, memoProgram: MEMO_PROGRAM, swapV2DiscriminatorHex: SWAP_V2_DISCRIMINATOR.toString("hex") });
