import crypto from "node:crypto";
import { OPENBOOK_V2_PROGRAM, SPL_TOKEN_PROGRAM } from "./openbook-market-snapshot.js";
import { buildUnsignedLegacyTransaction } from "./transaction-simulation.js";

const SYSTEM_PROGRAM = "11111111111111111111111111111111";
const PLACE_TAKE_ORDER_DISCRIMINATOR = Buffer.from([3, 44, 71, 3, 26, 199, 203, 85]);
const I64_MAX = (1n << 63n) - 1n, U64_MAX = (1n << 64n) - 1n;

function integer(value, label, maximum = U64_MAX) { let parsed; try { parsed = BigInt(value); } catch { throw new Error(`${label} is invalid`); } if (parsed < 0n || parsed > maximum) throw new Error(`${label} is invalid`); return parsed; }
function i64(value) { const data = Buffer.alloc(8); data.writeBigInt64LE(value); return data; }
function meta(address, signer, writable) { if (typeof address !== "string" || !address) throw new Error("OpenBook execution account is missing"); return { address, signer, writable }; }

export function buildOpenBookPlaceTakeOrderInstruction({ quote, market, signer, penaltyPayer = signer, userBaseAccount, userQuoteAccount, openOrdersAdmin = null }) {
  const buy = quote?.inputMint === market?.tokenMint1 && quote?.outputMint === market?.tokenMint0, sell = quote?.inputMint === market?.tokenMint0 && quote?.outputMint === market?.tokenMint1;
  if (quote?.schemaVersion !== 1 || quote.protocol !== "openbook-v2" || quote.pool !== market?.address || !buy && !sell || quote.executionBoundary !== "analysis_only_quote" || quote.executable !== false || quote.safeForAutomation !== false || market.programId !== OPENBOOK_V2_PROGRAM || market.tokenProgram0 !== SPL_TOKEN_PROGRAM || market.tokenProgram1 !== SPL_TOKEN_PROGRAM || !["finalized_full_fixed_depth", "finalized_full_depth_with_validated_oracle_pegs"].includes(market.orderbookCoverage) || !Array.isArray(quote.levels) || !quote.levels.length || quote.levels.length > 255 || quote.stateSlot !== market.stateSlot || quote.bookSlot !== market.bookSlot || quote.oracleSlot !== market.oracleSlot || quote.balanceSlot !== market.balanceSlot || quote.currentSlot < quote.balanceSlot || market.openOrdersAdmin !== openOrdersAdmin) throw new Error("OpenBook execution quote evidence is invalid");
  const amount = integer(quote.amountInRaw, "OpenBook input amount"), consumed = integer(quote.consumedInRaw, "OpenBook consumed amount"), output = integer(quote.amountOutRaw, "OpenBook output amount"), baseLots = integer(quote.baseLotsFilled, "OpenBook base lots", I64_MAX), quoteLots = integer(quote.quoteLotsFilled, "OpenBook quote lots", I64_MAX), fee = integer(quote.takerFeeRaw, "OpenBook taker fee"), baseLot = integer(market.baseLotSizeRaw, "OpenBook base lot", I64_MAX), quoteLot = integer(market.quoteLotSizeRaw, "OpenBook quote lot", I64_MAX), price = integer(quote.levels.at(-1)?.priceLots, "OpenBook limit price", I64_MAX);
  if (!amount || !consumed || consumed > amount || !output || !baseLots || !quoteLots || !baseLot || !quoteLot || !price || baseLots * baseLot !== (buy ? output : consumed) || quoteLots * quoteLot !== (buy ? consumed - fee : output + fee)) throw new Error("OpenBook execution bounds are invalid");
  const maxQuoteLotsIncludingFees = (buy ? consumed + quoteLot - 1n : output + fee + quoteLot - 1n) / quoteLot;
  if (!maxQuoteLotsIncludingFees || maxQuoteLotsIncludingFees > I64_MAX) throw new Error("OpenBook execution bounds are invalid");
  const data = Buffer.concat([PLACE_TAKE_ORDER_DISCRIMINATOR, Buffer.from([buy ? 0 : 1]), i64(price), i64(baseLots), i64(maxQuoteLotsIncludingFees), Buffer.from([1, quote.levels.length])]);
  const accounts = [meta(signer, true, true), meta(penaltyPayer, true, true), meta(market.address, false, true), meta(market.marketAuthority, false, false), meta(market.bids, false, true), meta(market.asks, false, true), meta(market.tokenVault0, false, true), meta(market.tokenVault1, false, true), meta(market.eventHeap, false, true), meta(userBaseAccount, false, true), meta(userQuoteAccount, false, true), meta(market.oracleA ?? OPENBOOK_V2_PROGRAM, false, false), meta(market.oracleB ?? OPENBOOK_V2_PROGRAM, false, false), meta(SPL_TOKEN_PROGRAM, false, false), meta(SYSTEM_PROGRAM, false, false), meta(openOrdersAdmin ?? OPENBOOK_V2_PROGRAM, openOrdersAdmin != null, false)];
  return { programId: OPENBOOK_V2_PROGRAM, accounts, dataHex: data.toString("hex"), evidence: { market: market.address, stateSlot: quote.stateSlot, bookSlot: quote.bookSlot, oracleSlot: quote.oracleSlot, balanceSlot: quote.balanceSlot, quoteCurrentSlot: quote.currentSlot, side: buy ? "bid" : "ask", amountInRaw: amount.toString(), consumedInRaw: consumed.toString(), quotedOutputRaw: output.toString(), priceLots: price.toString(), maxBaseLots: baseLots.toString(), maxQuoteLotsIncludingFees: maxQuoteLotsIncludingFees.toString(), orderType: "immediate_or_cancel", matchLimit: quote.levels.length } };
}

export function prepareOpenBookPlaceTakeOrderSimulation({ quote, market, signer, penaltyPayer = signer, userBaseAccount, userQuoteAccount, openOrdersAdmin = null, inputPreAmountRaw, outputPreAmountRaw, minimumOutputRaw, recentBlockhash }) {
  const instruction = buildOpenBookPlaceTakeOrderInstruction({ quote, market, signer, penaltyPayer, userBaseAccount, userQuoteAccount, openOrdersAdmin }), transaction = buildUnsignedLegacyTransaction({ feePayer: signer, recentBlockhash, instructions: [instruction] }), inputPre = integer(inputPreAmountRaw, "OpenBook input balance"), outputPre = integer(outputPreAmountRaw, "OpenBook output balance"), consumed = integer(quote.consumedInRaw, "OpenBook consumed amount"), minimum = integer(minimumOutputRaw, "OpenBook minimum output"), quoted = integer(quote.amountOutRaw, "OpenBook quoted output"), inputAccount = quote.inputMint === market.tokenMint0 ? userBaseAccount : userQuoteAccount, outputAccount = quote.outputMint === market.tokenMint0 ? userBaseAccount : userQuoteAccount;
  if (!minimum || minimum > quoted || inputPre < consumed || outputPre + quoted > U64_MAX) throw new Error("OpenBook simulation balance bounds are invalid");
  const prepared = { schemaVersion: 1, type: "openbook_place_take_order_simulation", protocol: "openbook-v2", commitment: "finalized", minContextSlot: Math.max(quote.stateSlot, quote.bookSlot, quote.oracleSlot, quote.balanceSlot, quote.currentSlot), transaction, instructionEvidence: instruction.evidence, simulationPolicy: { allowedProgramIds: [OPENBOOK_V2_PROGRAM], requiredProgramIds: [OPENBOOK_V2_PROGRAM], instructionPolicies: transaction.instructionPolicies, accountExpectations: [{ address: inputAccount, mint: quote.inputMint, preAmountRaw: inputPre.toString(), minDeltaRaw: (-consumed).toString(), maxDeltaRaw: (-1n).toString() }, { address: outputAccount, mint: quote.outputMint, preAmountRaw: outputPre.toString(), minDeltaRaw: minimum.toString(), maxDeltaRaw: quoted.toString() }] } };
  prepared.preparationHash = crypto.createHash("sha256").update(JSON.stringify(prepared)).digest("hex"); return prepared;
}

export const OPENBOOK_EXECUTION_CONSTANTS = Object.freeze({ placeTakeOrderDiscriminatorHex: PLACE_TAKE_ORDER_DISCRIMINATOR.toString("hex"), systemProgram: SYSTEM_PROGRAM });
